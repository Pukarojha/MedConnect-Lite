<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Demo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Disable default action bar and title -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Customize your app theme -->
        <item name="colorPrimary">@color/themeColor</item>
    </style>

    <style name="Theme.Demo" parent="Base.Theme.Demo">
        <!-- You can override additional items here if needed -->
    </style>

    <style name="welcome_screen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize welcome screen separately if needed -->
        <item name="colorPrimary">@color/themeColor</item>
    </style>
</resources>
